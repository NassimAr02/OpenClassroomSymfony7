<!DOCTYPE html>
<html>
    <head>
        <!-- Déclare que le document utilise HTML5 -->
        <meta charset="utf-8" />
        <!-- Configure l'affichage pour être adapté aux écrans mobiles -->
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
        <!-- Description du site (utile pour le SEO) -->
        <meta name="description" content="" />
        <!-- Auteur du site -->
        <meta name="author" content="" />
        <!-- Titre de la page (par défaut), modifiable via le bloc Twig -->
        <title>Biblios - {% block title %}{% endblock %}</title>
        <!-- Icône du site (favicon) en SVG -->
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">

        <!-- Bloc Twig pour insérer des styles spécifiques à chaque page -->
        {% block stylesheets %}
        {% endblock %}

        <!-- Bloc Twig pour insérer des scripts spécifiques à chaque page -->
        {% block javascripts %}
            <!-- Import des modules JavaScript via Importmap -->
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </head>
    <body id="page-top">
        <!-- Barre de navigation (menu principal) -->
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top" id="mainNav">
            <div class="container px-4">
                <!-- Nom ou logo du site cliquable qui ramène en haut de la page -->
                <a class="navbar-brand" href="#page-top">Biblios</a>
                <!-- Bouton pour afficher/masquer le menu sur les petits écrans -->
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation"><span class="navbar-toggler-icon"></span></button>
                <!-- Menu déroulant avec des liens -->
                <div class="collapse navbar-collapse" id="navbarResponsive">
                    <ul class="navbar-nav ms-auto">
                        <!-- Lien vers le catalogue -->
                        <li class="nav-item"><a class="nav-link" href="#">Catalogue</a></li>
                        <!-- Lien vers la page de connexion -->
                        <li class="nav-item"><a class="nav-link" href="#">Se connecter</a></li>
                    </ul>
                </div>
            </div>
        </nav>
        <!-- Bloc Twig pour insérer le contenu spécifique de chaque page -->
        {% block body %}{% endblock %}
        <!-- Pied de page -->
        <footer class="py-5 bg-dark">
            <!-- Contenu du footer avec un copyright centré -->
            <div class="container px-4">
                <p class="m-0 text-center text-white">Copyright &copy; Biblios</p>
            </div>
        </footer>
    </body>
</html>
